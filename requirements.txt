# Recommended environment:
#   Python 3.12
# If you already have a virtual environment or conda environment:
#   1) activate that environment
#   2) python -m pip install --upgrade "pip<26" "setuptools==81.0.0" wheel
#   3) python -m pip install -r requirements.txt
#
# Notes:
# - Some HWP workflows still work better with system tools such as `hwp5txt` or `libreoffice`.
# - `bitsandbytes` is intentionally excluded because it is GPU/CUDA sensitive.
# - `kiwipiepy` requires a C++ build toolchain on some platforms (apt install build-essential).

# Core data processing
pandas
numpy
rank-bm25

# Document parsing
olefile               # HWP binary container parsing
pymupdf               # PDF parsing (imported as fitz)
beautifulsoup4        # HWP HTML/XML text extraction (imported as bs4)

# Korean NLP
kiwipiepy             # Kiwi morphological analyzer — BM25 tokenization

# ML / Reranking
torch
transformers          # Cross-Encoder reranker (BAAI/bge-reranker-v2-m3)
sentence-transformers # Embedding model loader

# Vector stores
faiss-cpu

# LangChain stack
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-openai

# API
openai
python-dotenv

# App / serving
streamlit
streamlit-pdf-viewer

# Schema validation
pydantic

# Dev / testing
ipykernel
pytest
